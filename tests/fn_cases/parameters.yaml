request:
  input:
    step: pytest
    parameters:
      hello: world
    composite: |
      class Composite(BaseComposite):
        def compose(self):
          cm = self.resources.ConfigMap('ConfigMap', 'v1')
          cm.data = self.parameters

response:
  desired:
    resources:
      ConfigMap:
        resource:
          apiVersion: v1
          kind: ConfigMap
          data:
            hello: world
